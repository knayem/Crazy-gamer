import React from 'react';
import OrderInfo from '../OrderInfo/OrderInfo';

const Order = () => {
    return (
        <div>

          <OrderInfo></OrderInfo>

            
        </div>
    );
};

export default Order;